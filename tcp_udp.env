# ============================================================
#  Locust DP GUI - TCP/UDP Configuration
# ============================================================
# This file is generated by the GUI.
# Keep the filename lowercase: tcp_udp.env
# ============================================================


# ============================================================
#  TCP STAGES
# ============================================================

TCP_STAGES='[{"duration": 60, "users": 10, "spawn_rate": 1, "packet_size": 60}]'

# ============================================================
#  UDP STAGES
# ============================================================

UDP_STAGES='[{"duration": 60, "users": 10, "spawn_rate": 1, "packet_size": 60}, {"duration": 120, "users": 20, "spawn_rate": 1, "packet_size": 60}, {"duration": 180, "users": 30, "spawn_rate": 1, "packet_size": 60}]'

# ============================================================
#  TCP PARAMETERS
# ============================================================

TCP_WORKERS='-1'
TCP_STOP_TIMEOUT='60'
TCP_TARGET_RPS='-1'

# ============================================================
#  UDP PARAMETERS
# ============================================================

UDP_WORKERS='-1'
UDP_STOP_TIMEOUT='60'
UDP_TARGET_RPS='-1'

# ============================================================
#  SHARED TCP/UDP
# ============================================================

PROTOCOL='TCP'
IPADDR=''
WORKERS='-1'
PACKET_SIZE='60'
TIME_TOTAL='60'
SPAWN_RATE='1'
SCAN_PORT_RANGE_TCP_START='12000'
SCAN_PORT_RANGE_TCP_END='13000'
SCAN_PORT_RANGE_UDP_START='12000'
SCAN_PORT_RANGE_UDP_END='13000'
SOURCE_IP_MINIMAL='192.168.100.73'
SOURCE_IP_MAXIMAL='192.168.100.74'
UNIQUE_USERS_COUNT='10'
POLL_INTERVAL='5'
INTERVAL='5'
